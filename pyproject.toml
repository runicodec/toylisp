[project]
name = "toylisp"
version = "0.0.67"
description = "Minimal LISP implementation in Python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.2",
]

[pytest]
addopts = ["--import-mode=importlib"]
